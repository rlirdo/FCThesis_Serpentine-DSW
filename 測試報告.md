# 測試報告：花蓮綠色化學闖關（WebAR）

- 專案路徑：`D:\01-FC東華\04-專案\AR遊戲_花蓮綠色化學闖關\`
- 版本：**v1.3**（2026/08/31 萬用卡／卡片輪播頁／免卡體驗——解決每關要換一張圖的斷點）
- 線上網址：https://rlirdo.github.io/FCThesis_Serpentine-DSW/
- **分享用網址（建議一律用這個）**：https://rlirdo.github.io/FCThesis_Serpentine-DSW/?openExternalBrowser=1
- 測試日期：2026/08/31
- 測試環境：Windows 11、Chromium 核心瀏覽器面板、視窗模擬 375 × 812（iPhone X 尺寸）
- 測試方式：本機 `python tools/devserver.py 8765` → `http://localhost:8765`
- 測試者：Claude（執行代理）

---

## 一、已驗證項目（本機可驗證，全部通過）

### 1. 載入與函式庫

| 項目 | 結果 |
|---|---|
| A-Frame 載入 | ✅ 1.5.0 |
| MindAR 載入 | ✅ `window.MINDAR.IMAGE` 存在 |
| `mindar-image-system` 註冊 | ✅ |
| `mindar-image-target` 元件註冊 | ✅ |
| 安全來源檢查（secure context） | ✅ localhost 判定為安全來源 |
| 素材預載（3 主角 ＋ 10 場景） | ✅ 13/13 |

### 2. AR 追蹤資料（`.mind`）

10 個檔案全部以 MindAR 官方 `Compiler.importData()` 實際解析驗證：

| 關卡 | 檔案大小 | 目標數 | 原圖尺寸 | keyframe 數 | 首張 keyframe 特徵點 | 全部特徵點 |
|---|---|---|---|---|---|---|
| 1 | 664 KB | 1 | 1200×1200 | 12 | 436 | 3283 |
| 2 | 692 KB | 1 | 1200×1200 | 12 | 430 | 3456 |
| 3 | 806 KB | 1 | 1200×1200 | 12 | 453 | 4087 |
| 4 | 651 KB | 1 | 1200×1200 | 12 | 420 | 3225 |
| 5 | 736 KB | 1 | 1200×1200 | 12 | 453 | 3730 |
| 6 | 711 KB | 1 | 1200×1200 | 12 | 452 | 3557 |
| 7 | 716 KB | 1 | 1200×1200 | 12 | 439 | 3602 |
| 8 | 647 KB | 1 | 1200×1200 | 12 | 421 | 3208 |
| 9 | 711 KB | 1 | 1200×1200 | 12 | 430 | 3570 |
| 10 | 727 KB | 1 | 1200×1200 | 12 | 440 | 3657 |

> MindAR 的經驗值是單一 keyframe 特徵點 **超過約 100 即屬良好**。
> 本專案 10 張圖皆在 **420–453** 之間，特徵密度充裕，
> 顯示「不規則刻度邊框＋四角唯一識別碼＋非重複紋理斑塊」的設計達到預期效果。
> 每張圖的四角識別碼皆不同，可避免 10 張圖彼此誤判。
>
> **v1.3 起這張表已過時**：十個 `.mind` 全部重編為**雙目標**（各含該關卡片與遊戲萬用卡），
> 檔案大小約 1.4–1.6 MB、目標數為 2。最新數據見「二之四、v1.3」。

### 3. AR 場景 3D 教學內容（不需相機即可驗證）

把 10 關的 3D 教學內容實際丟進一個真實的 A-Frame 場景實例化（不啟動相機）：

| 關卡 | 內容型別 | 子物件數 | 成功實例化 |
|---|---|---|---|
| 1 | resources | 6 | 6 ✅ |
| 2 | waste | 9 | 9 ✅ |
| 3 | layers | 22 | 22 ✅ |
| 4 | ions | 6 | 6 ✅ |
| 5 | water | 4 | 4 ✅ |
| 6 | micelle | 2 | 2 ✅ |
| 7 | energy | 4 | 4 ✅ |
| 8 | product | 5 | 5 ✅ |
| 9 | mineral | 5 | 5 ✅ |
| 10 | twelve | 13 | 13 ✅ |

- A-Frame renderer 與 canvas 皆成功建立 ✅
- 全部標記語法通過 XML 解析，無語法錯誤 ✅
- `a-text` 的文字內容**全部為 ASCII** ✅
  （刻意如此：A-Frame 預設 MSDF 字型不含中文字符，中文一律走 HTML 教學面板，
  避免在 AR 中出現豆腐字）

### 4. 遊戲流程（10 關完整跑完，兩次獨立回歸）

| 項目 | 結果 |
|---|---|
| 標題頁 → 玩法說明 → 返回 | ✅ |
| 主角三選一（阿海／小玉／蛇紋獸）與選取狀態 | ✅ 三位皆可選，未選時「下一步」為停用 |
| 關卡數選擇 5 關／10 關 | ✅ 5 關版正確取 ①③⑤⑥⑧ |
| 探索模式場景載入（10 張背景） | ✅ 10/10 |
| 虛擬方向鍵移動（`pointerdown`／`pointerup`） | ✅ 右鍵 0.9 秒位移 20.4%，符合 SPEED=26%/秒 |
| 鍵盤方向鍵（桌機測試用） | ✅ |
| 可行走垂直範圍鉗制 | ✅ 依場景地形與實際 UI 位置雙重鉗制，未走進海裡、天空或方向鍵底下 |
| 走到目標點觸發 | ✅ 對白換成任務提示、目標點由「?」轉「!」、「開始掃描」出現 |
| 離開目標點還原 | ✅ |
| 掃描頁進入與關卡標題／關鍵圖對應 | ✅ 10/10 |
| 教學面板 2D 示意圖 | ✅ 10/10 皆渲染出 SVG |
| 教學面板重點條列 | ✅ 10/10 各 3 條 |
| 問答：答錯 → 顯示提示、該選項鎖定、可續答 | ✅ 10/10 |
| 問答：答對 → 顯示解說、全部選項鎖定、出現「過關！」 | ✅ 10/10 |
| 關卡總結核心觀念 | ✅ 10/10 |
| 全破總結頁：徽章、正確率、五大概念、逐關回顧 | ✅ 5 個概念卡、10 條回顧 |
| 正確率計算 | ✅ 全對＝100%；每關先錯一次再答對＝50%（10/20），計算正確 |

### 5. 相機降級路徑（**本機無可用相機，正好完整驗證**）

| 項目 | 結果 |
|---|---|
| 「開始掃描（開啟相機）」僅由按鈕 click 觸發 | ✅ 未在任何非手勢情境呼叫 `getUserMedia` |
| 相機取得失敗時自動切換降級面板 | ✅ |
| 失敗訊息明確 | ✅「相機沒有畫面，可能是未允許權限，或被其他 App 佔用。」 |
| 「用圖片模式繼續」 | ✅ 顯示該關關鍵圖片與完整教學內容 |
| 「跳過掃描，直接答題」 | ✅ 教學內容照樣完整，可正常過關 |
| 「再試一次相機」 | ✅ 可重複重試，連續兩次失敗仍無錯誤 |
| 失敗後資源清理 | ✅ `a-scene` 與 `video` 元素皆歸零，無殘留 |

### 6. UI 遮蔽檢查（方向鍵／掃描鈕不得蓋住主角與目標點）

自動化檢查方式：每一關把主角放到可行走區的**四個角落、四個邊界中點**共 8 個位置，
再加上**目標點（此時掃描鈕已出現）**，逐次比對
`hero.getBoundingClientRect()`、`#goal` 與**每一顆 `.dbtn`**（以及掃描鈕）的矩形是否相交。

| 視窗尺寸 | 關卡數 | 每關取樣點 | 總檢查數 | `heroUnderPad` | `goalUnderPad` | `heroUnderScan` | `goalUnderScan` |
|---|---|---|---|---|---|---|---|
| 375 × 812 | 10 | 9 | 90 | 全部 false ✅ | 全部 false ✅ | 全部 false ✅ | 全部 false ✅ |
| 414 × 896 | 10 | 9 | 90 | 全部 false ✅ | 全部 false ✅ | 全部 false ✅ | 全部 false ✅ |

實測到的可行走下緣會依螢幕自動調整（375×812 為 **72.6%**、414×896 為 **75.2%**），
證明限制是依實際版面量測而非寫死百分比。

另以**真實方向鍵事件**完整走完 10 關（每關先右移再下移到底），
全程持續偵測重疊：**`overlapSeenDuringPlay: false`**、錯誤 0 筆。

視窗尺寸變動後（414×896 → 375×812）自動 relayout：主角與目標點皆自動收回安全範圍 ✅

### 7. 響應式與觸控（375 × 812）

| 項目 | 結果 |
|---|---|
| 所有按鈕觸控尺寸 ≥ 44 px | ✅ 全站 27 個按鈕，無一低於 44 px |
| 方向鍵尺寸 | ✅ 64 × 64 px |
| 頁面水平溢出 | ✅ `document.body.scrollWidth` = 375 = 視窗寬，無橫向捲動 |
| 逐畫面截圖檢查 | ✅ 標題／玩法／主角／關卡數／探索／掃描／降級／教學／問答／關卡總結／全破，共 11 個畫面皆不破版 |

### 8. JavaScript 錯誤

以全新分頁載入、並在頁內掛 `error` 與 `unhandledrejection` 監聽器實測：

- 完整 10 關跑完（含每關先答錯再答對）：**錯誤 0 筆**
- 連續兩次相機啟動失敗：**錯誤 0 筆**
- 主控台 `onlyErrors` 讀取：**乾淨**

---

## 二、測試過程中找到並修正的問題

1. **相機靜默失敗（重要）**
   MindAR 的 `system.start()` 在相機被拒絕或被佔用時**仍會 resolve**，
   `video.videoWidth` 卻永遠是 0。若不處理，使用者會卡在一片黑的掃描畫面。
   → 已在 `js/ar.js` 加入串流健康檢查：8 秒內沒有實際畫面即判定失敗並轉降級路徑。

2. **關閉 AR 時的未攔截例外（重要）**
   MindAR 的 `mindar-image` 元件在 `remove()` 時會再呼叫一次 `system.stop()`，
   而該實作直接存取 `video.srcObject.getTracks()` 與 `controller.stopProcessVideo()`，
   相機從未啟動時兩者為 null／undefined，丟出 `TypeError`。
   → 已在 `AR.stop()` 自行完成收拾後，把 `sys.stop`／`sys.pause` 換成無動作函式。

3. **標題頁裝飾圓遮住標題文字** → `.pad` 加 `position:relative; z-index:1`。

4. **關卡數選擇卡「精華版／完整版」被擠成兩行** → `.mode-card` 由 flex 改 grid，
   數字圓跨兩列，標題與說明改為上下堆疊。

5. **場景名牌與虛擬方向鍵重疊**（直式手機以 `cover` 顯示時名牌落在畫面最下方）
   → 移除場景 SVG 內的名牌，地點名稱改由畫面上方 HUD 顯示。

6. **目標點落在海面／天空**（原座標未對應場景地形）
   → 重新依場景 SVG 的實際地形設定每關的目標點、起點與可行走垂直範圍。

7. **第 1 關關鍵圖片標語壓在山稜上不易閱讀** → 移到天空區並加深色膠囊底。
   （關鍵圖片一經修改即重新編譯該關 `.mind`，確保追蹤資料與列印圖一致。）

8. **A-Frame 載入順序警告** → 四支腳本改置於 `<head>` 並全部加 `defer`，
   順序保留且不阻擋載入畫面繪製。

9. **方向鍵壓住主角與目標點（主會話回報，最初的自動 QA 未涵蓋）**
   起因是可行走範圍的下緣寫死成百分比，但方向鍵是**固定像素高度**——
   同一組百分比在不同螢幕上對應的位置不同，主角因此走進按鍵底下。
   修正採**三管齊下**：
   - 可行走下緣改由 `walkLimits()` **在執行期量測 `.hud-bottom` 的實際位置**再換算成百分比
     （主角以腳底定位、目標點以圓心定位，各自預留 10 px 安全間距與 42 px 半徑）；
     上緣同樣依對白框實際下緣量測，避免主角被對白框蓋住。
   - 掃描鈕改用 `off` 類別（僅隱藏可見度、**保留版面高度**），
     使可行走區不會在按鈕出現時突然縮小。
   - 目標點座標一律經 `goalPos()` 依同一限制夾擠，並在 `resize`／`orientationchange` 時重新套用。

   修正過程中**又發現第二個遮蔽來源**：掃描鈕本身會蓋住第 1、3、4、9 關的目標點。
   根因是這幾張場景的地形畫在畫面最底部（如七星潭礫石灘原本從 79% 才開始），
   整片可行走地形都落在 UI 底下。
   → 已重畫場景 1、3、4、8、9、10，把地面／道具抬高到畫面 62% 附近，
   讓每一關都有**看得見、走得到**的活動空間（同時畫面也比原本開闊）。

---

## 二之二、v1.1 相機故障修復（2026/08/31 實機回報後）

### 使用者實測回報

> 手機按「開始掃描（開啟相機）」後，**出現黃色掃描框但框內全黑**，幾秒後被健康檢查轉入降級模式。
> 追問後確認：**使用者是從 LINE 點開網址的。**

### 根因

| 序 | 根因 | 說明 |
|---|---|---|
| **1（主因）** | **LINE 內建瀏覽器封鎖相機** | LINE 的 in-app WebView 不提供 `getUserMedia` 的相機權限，取得的串流沒有影像 → 黃框內全黑 |
| 2（潛在） | **iOS 手勢鏈斷裂** | v1.0 流程是「按鈕 → 先載 `.mind`（600–800 KB，行動網路 1–3 秒）→ MindAR 內部才 `getUserMedia`／`play()`」，此時早已脫離使用者手勢，iOS 會拒絕播放 |
| 3（潛在） | **二次 `getUserMedia`** | MindAR 自己會再開一次相機；Android 在前一條串流尚未釋放時常回 `NotReadableError` |
| 4 | **不說原因就降級** | v1.0 只丟一句「相機沒有畫面」，使用者與開發者都無從判斷是權限、鏡頭佔用還是真的全黑 |

### 修正對照表

| 代號 | 修正 | 實作位置 |
|---|---|---|
| **C** | LINE UA → 自動導向 `?openExternalBrowser=1`（保留原 query 與 hash；已帶參數不再導向） | `js/ar.js` `detectInApp()` / `externalBrowserUrl()` / `escapeInAppBrowser()`，檔尾自動執行 |
| **C** | FB／IG／微信 → 置頂紅色橫幅 ＋ 掃描面板常駐提示 | `js/game.js` `inAppBanner()`；`index.html` `#inapp-banner`、`.ar-warn` |
| **A** | 按鈕手勢內**立刻** `getUserMedia` → 接上 `#cam-preview` 立即出畫面 → 背景載 `.mind` → 顯示「正在啟動 AR…」再交棒 | `js/game.js` `startCamera()`；`js/ar.js` `acquireCamera()` / `attachPreview()` / `prefetchTarget()` |
| **A** | `.mind` 轉 blob URL 交給 MindAR，不再二次下載 | `js/ar.js` `prefetchTarget()` |
| **A/F** | 覆寫 MindAR `_startVideo`，沿用同一條串流 —— **全程只呼叫一次 `getUserMedia`** | `js/ar.js` `start()` 內的 `sys._startVideo` 覆寫 |
| **B** | MindAR 的 video 強制 `playsinline`／`webkit-playsinline`／`muted` ＋ 主動 `play()`（rejection 記入診斷） | `js/ar.js` `fixVideo()` / `forcePlay()` / `watchVideos()` / `repatch()` |
| **D** | `navigator.permissions.query({name:'camera'})` 權限狀態 | `js/ar.js` `queryPermission()` |
| **D** | 五類 `getUserMedia` 錯誤 → 對應中文訊息 ＋ 各平台開權限指引 | `js/ar.js` `explain()` |
| **D** | 4×4 canvas 平均亮度；連續 3 秒 < 6 判定黑畫面 | `js/ar.js` `brightnessOf()` / `probe()` |
| **D** | 「📋 複製診斷資訊」按鈕（剪貼簿不可用時退回文字方塊） | `js/game.js` `copyDiag()`；`js/ar.js` `diagText()` |
| **E** | 寬限 8 → **12 秒**，顯示倒數與「繼續等待」；亮度 > 6 立即解除 | `js/game.js` `healthCheck()` |
| **E** | 降級不自動跳轉，改顯示原因 ＋ 三選項（重試相機／圖片模式／直接答題） | `js/game.js` `failCamera()`；`index.html` `#ar-fallback` |
| **F** | `facingMode: { ideal: 'environment' }`；`OverconstrainedError` 降級 `{video:true}` 重試一次 | `js/ar.js` `acquireCamera()` |
| **G** | 開場頁角落 `v1.1`；js/css 加 `?v=1.1` 破快取 | `index.html`、`css/style.css` `.ver-tag` |

### v1.1 驗證結果（本機，2026/08/31）

| 驗證項目 | 方法 | 結果 |
|---|---|---|
| 內建瀏覽器偵測（12 組 UA） | `node tools/test_inapp.js` 純函式單元測試 | ✅ LINE／LIFF／FB(iOS+Android)／IG／微信全中；Safari／Chrome(iOS+Android)／桌機／`Skyline/` 皆不誤判 |
| LINE 逃生網址 | 同上 | ✅ 補參數、保留 query、保留 hash、query+hash 並存、已帶參數不重導（防無限迴圈）、壞網址不炸 |
| 錯誤訊息對應 | 同上 | ✅ 7 種錯誤名稱皆有中文原因 ＋ 指引 |
| 單元測試總計 | `node tools/test_inapp.js` | ✅ **39 項全數通過** |
| LINE 重導端對端 | 瀏覽器內覆寫 `navigator.userAgent` 後實際導向 | ✅ `?utm=poster#lv3` → `?utm=poster&openExternalBrowser=1#lv3`；再呼叫回 `null`，不重複導向 |
| 內建瀏覽器橫幅 | 同上（IG／LINE 兩種 UA） | ✅ 橫幅顯示、文案正確、內容自動下推不被遮住、可關閉 |
| 相機權限被拒的診斷路徑 | 瀏覽器面板封鎖相機，走真實失敗路徑 | ✅ `NotAllowedError` → 標題「相機無法使用」＋ 原因「相機權限被拒絕…」＋ iPhone／Android 開權限指引 ＋ 三個選項 |
| 複製診斷資訊 | 點按鈕 | ✅ 產出 18 行完整診斷；剪貼簿被封鎖時自動退回文字方塊並提示長按複製 |
| 黑畫面偵測 | `canvas.captureStream()` 假造全黑串流 | ✅ 亮度 0；`darkRun` 1→2→3，第 3 秒 `black=true` |
| 亮度解除 | 同上改為亮色畫面 | ✅ 亮度 204、`bright=true`、`darkRun` 歸零 |
| 健康檢查倒數 | 無畫面時觀察 | ✅ 每秒遞減，顯示「正在等待相機畫面… N 秒」＋「繼續等待」按鈕 |
| 「繼續等待」 | 點按鈕 | ✅ 倒數由 10 秒重設回 12 秒；倒數結束後不再給假回饋 |
| 健康檢查解除 | 亮串流下執行 | ✅ 1.8 秒內回 `{ok:true}`，倒數 UI 收起、狀態文字復原 |
| **MindAR 串流交棒（核心）** | 攔截 `navigator.mediaDevices.getUserMedia` 計次 | ✅ **`gumCalls: 0`** —— MindAR 沒有再開第二次相機，沿用預檢串流 |
| MindAR video 屬性 | 交棒後檢查 | ✅ 640×480、`paused:false`、`muted:true`、`playsinline`＋`webkit-playsinline` 皆存在、`play()` 無 rejection |
| `.mind` blob 交付 | 檢查 `imageTargetSrc` | ✅ `blob:` URL，背景預載成功 |
| AR 停止與清理 | `AR.stop()` | ✅ scene／video 移除、串流歸還、預覽收起、`running:false`、零例外 |
| 10 關完整回歸 | 走完 10 關（每關含答錯一次再答對） | ✅ 順序 1–10、教學 SVG／重點、答錯提示、答對回饋、關卡結算、徽章與 10 條回顧、5 個帶走概念 |
| 5 關精華版回歸 | 走「直接答題」路徑 | ✅ 順序 1,3,5,6,8、正確率 100%、5 條回顧 |
| JavaScript 錯誤 | 全程 console | ✅ **零錯誤** |

---

## 二之三、v1.2 掃描成功卻停在原地（2026/08/31 第二次實機回報）

### 使用者實測回報

> 相機正常、MindAR 成功辨識關鍵圖片、3D 教學內容（四個柱狀體＋mountain／ocean 文字）
> 正確疊加在實境畫面上，**但之後停在原地，無法進入下一步（教學面板→答題）**。

### 根因（診斷選項 c 的變形：事件有綁到，但 callback 被自己抹掉）

不是 z-index 被蓋住（a），也不是穩定追蹤計時器被 targetLost 重置（b），
更不是元素 id 對不上（事件其實有正常觸發）。真正的斷點在 `js/ar.js` 的 `start()`：

```js
host = opts.host;
onFoundCb = opts.onFound;          // ① 先存起來
...
await stop({ keepStream: true });  // ② stop() 開頭無條件 onFoundCb = null
```

`start()` 會先把 `opts.onFound` 存進模組變數 `onFoundCb`，緊接著才呼叫
`stop({ keepStream: true })` 做「開始前的清場」；而 `stop()` 第一行就是
`onFoundCb = null`。於是**掃描還沒開始，銜接遊戲流程的 callback 就已經被清成 null**。

後來 `targetFound` 真的觸發時，監聽器裡的 `if (onFoundCb)` 永遠不成立：

```js
tgt.addEventListener('targetFound', () => {
  log('targetFound level', level.n);
  if (onFoundCb) { ... }           // ← 永遠是 null，什麼都不做
});
```

3D 內容仍然會浮現，是因為那是 MindAR `mindar-image-target` 元件自己控制的
（找到目標就把 entity setAttribute('visible', true)），**與遊戲狀態機無關**。
所以畫面上看起來「AR 成功了」，實際上遊戲流程從頭到尾沒有前進 —— 完全符合回報症狀。

### 修正對照表

| # | 檔案 | 修正 |
|---|---|---|
| 1 | `js/ar.js` | **根因**：`onFoundCb` 改在 `await stop({keepStream:true})` **之後**才掛上；且 `stop({keepStream:true})` 不再清除 callback（只有真正結束掃描才清） |
| 2 | `js/ar.js` | `targetFound`／`targetLost` 同時綁在 **entity（`#ar-target`）與 `a-scene` 兩層**，涵蓋 MindAR 版本差異；A-Frame 事件會冒泡，用 `foundFired` 與 `e.target` 檢查去重 |
| 3 | `js/ar.js` | 觸發次數改由 `foundFired` 控制，**不再靠「用完就把 callback 設 null」**，避免同類錯誤再發生 |
| 4 | `js/ar.js` | 新增 `simulateFound()`／`simulateLost()`／`hasFoundCallback()` 驗證掛鉤；診斷資訊加入 targetFound／targetLost 次數與 callback 是否觸發 |
| 5 | `index.html`、`css/style.css` | 新增固定定位的掃描成功層：綠色橫幅「✓ 掃描成功！」＋倒數，底部大型主按鈕「進入教學 ▶」（`position:fixed`、z-index 9001／9002，壓過 a-scene、a-canvas 與所有既有面板） |
| 6 | `js/game.js` | `onFound` 改接 `scanSuccess()`：橫幅＋`navigator.vibrate(80)`＋大按鈕＋**4 秒倒數自動進入**；按鈕可隨時提前進入 |
| 7 | `js/game.js` | `onLost` 明確設為 no-op：**targetLost 不重置倒數、不收回按鈕** —— 掃到就算數，手持晃動不懲罰玩家 |
| 8 | `js/game.js` | 進教學前才 `await AR.stop()`：倒數的 4 秒內 3D 疊加內容持續顯示，進教學時才關閉 MindAR 與相機串流 |
| 9 | `css/style.css` | **動畫只碰 `box-shadow`，位置一律由靜態 CSS 決定**（見下方「順手修掉的第二個坑」） |

### 順手修掉的第二個坑：用動畫做位移的主按鈕會被裁掉

第一版把進場效果寫成 `@keyframes teachRise { from{ transform: translateX(-50%) translateY(28px) } ... }`。
驗證時量到按鈕底部**超出畫面 4px**（`gapBottom: -4`），追查後發現：
分頁在背景時（`document.hidden === true`）CSS 動畫被凍結在 `currentTime: 0`，
`animation-fill-mode: both` 讓元素停在 `from` 的 `translateY(28px)`，
整顆主按鈕就往下偏 28px。同樣情況也會發生在 `prefers-reduced-motion`、低耗電模式。

**主要行動按鈕的位置絕對不能依賴動畫跑完。** 已改為：位置由靜態 CSS 決定，
動畫只做「發光呼吸」（`box-shadow`），並補上 `@media (prefers-reduced-motion: reduce)` 關閉動畫。
修正後量到 `gapBottom: 24`（＝設計值），動畫凍結與否都一樣。

### v1.2 驗證結果（本機，2026/08/31，375 × 812）

驗證手法：以 `canvas.captureStream(30)` 覆寫 `navigator.mediaDevices.getUserMedia`
產生**真的 MediaStream**，讓 `startCamera()` → `AR.start()` → MindAR 走完整條真實路徑
（`stage: mindarAR`、亮度 181、`mindReady: true`），再用 `AR.simulateFound()`
在 entity／scene 兩層各派發一次真的 `targetFound` 事件。

| 驗證項目 | 結果 |
|---|---|
| callback 是否還在（v1.1 的斷點） | ✅ `AR.hasFoundCallback() === true`（v1.1 在此為 `false`） |
| targetFound（**entity 層**） | ✅ `diag.foundCallback: 'fired'`、`targetFound: 1` |
| targetFound（**scene 層**） | ✅ 同樣成功觸發，冒泡不會重複計數 |
| 綠色橫幅 | ✅ 「✓ 掃描成功！」，`top: 14px`，z-index 9001 |
| 震動回饋 | ✅ `navigator.vibrate(80)` 被呼叫一次 |
| 大型主按鈕 | ✅ 330 × 72、`position: fixed`、z-index 9002、距底 24px |
| 按鈕是否被 a-scene 蓋住 | ✅ `elementFromPoint()` 在按鈕中心回傳 **`btn-ar-teach` 本身**（最上層可點） |
| 4 秒倒數 | ✅ 橫幅顯示 `4… → 3… → 2… → 1…`，每秒一格 |
| 自動進入教學 | ✅ 倒數結束自動切到教學面板 |
| **targetLost 不干擾** | ✅ 倒數中連發 3 次 targetLost：倒數停在原格不重置、橫幅與按鈕都不收回 |
| 3D 內容維持顯示 | ✅ 倒數 4 秒期間 `AR.sceneEl` 與 `#ar-target` 仍在，`AR.running: true` |
| 手動提前進入 | ✅ 倒數第 2 秒按「進入教學 ▶」立即進入，計時器清乾淨，不會再自動跳一次 |
| **相機確實關閉** | ✅ 進教學後 `AR.running: false`、a-scene 已移除、所有 `MediaStreamTrack.readyState === 'ended'`、無任何 `<video>` 還掛著 `srcObject` |
| 10 關完整回歸（圖片模式） | ✅ 10 關全過，每關教學 3 條重點＋1 張 SVG、4 選項、答對回饋、關卡結算；徽章、10 條回顧、5 個帶走概念齊全 |
| 5 關精華版＋答錯重試 | ✅ 順序 1,3,5,6,8；答錯給提示並鎖住該選項，再答對正常過關 |
| JavaScript 錯誤 | ✅ **全程零錯誤**（含 `error`／`unhandledrejection`／`console.error`） |


---

## 二之四、v1.3 每一關都要換一張圖（2026/08/31 第三次實機回報）

### 使用者實測回報

> 第 1 關全流程成功（掃描 → 3D 浮現 → 進教學 → 答題 → 過關），
> 但進第 2 關時**必須回筆電切到第 2 張圖才能掃**，體驗在關與關之間被切斷。

v1.2 修好的是「掃到之後不會前進」，這一次不是故障，是**設計本身的斷點**：
每一關的 `.mind` 只含一個 target，玩家手上的那張紙（或筆電畫面）只對得上一關。
現場只要有一個人負責換圖，整條動線就得停下來等他。

### 三條並行的解法（全部實作，玩家可自由選）

| # | 解法 | 做法 | 效果 |
|---|---|---|---|
| ① | **遊戲萬用卡** | 新增第 0 張卡 `assets/targets/universal.png`；十個 `.mind` **全部重編成雙目標**（target 0 ＝該關專屬卡、target 1 ＝萬用卡），遊戲端兩個 targetIndex 都接到同一個 `scanSuccess` | 一張卡從第 1 關玩到第 10 關，完全不必換圖 |
| ② | **卡片輪播頁 `cards.html`** | 全白底、大箭頭（≥64 px）、鍵盤 ←→、點左右半屏、觸控滑動、圓點索引 | 第二台裝置（另一支手機／平板／筆電）當卡片用，換卡不必碰檔案 |
| ③ | **免卡體驗模式** | 不用 MindAR、不用錨定：後鏡頭實景全螢幕＋教學內容半透明疊加，並可「📸 拍下學習瞬間」合成照片 | 手邊完全沒有卡片也能玩完，還有成果可以帶走 |

另外用 `localStorage` 記住上次選的模式，下一關預設高亮同一個並標示「上次用這個」。

### 萬用卡的設計約束

AR 影像追蹤怕兩件事：**特徵點太少**與**自我相似**（畫面出現規則重複的圖樣，
匹配器會對錯位置）。萬用卡因此刻意做成：

- 邊框改成**波浪＋菱格鏈＋六邊形角徽**（關卡卡是刻度線＋方形二進位角碼），一眼分得出來也不會互相誤配。
- 版心是**蛇紋石斜層理 × 海浪 × 三位主角剪影**；–OH 門把只放在部分層、間距不等，
  避免形成規則點陣（第一版門把過密，已改稀疏化）。
- 主色改森綠／典雅金，標頭是綠底（關卡卡是 navy 底）。

### v1.3 驗證結果（本機，2026/08/31，375 × 812）

驗證手法同 v1.2：以 `canvas.captureStream(30)` 覆寫 `getUserMedia` 產生真的 MediaStream，
走完整條 `startCamera() → AR.start() → MindAR` 路徑，再用 `AR.simulateFound()` 派發真事件。

**`.mind` 雙目標（`importData()` 實際讀檔驗證，10/10 全數通過）**

| 關卡 | 檔案大小 | target 數 | 特徵點（target 0 ／ target 1＝萬用卡） |
|---|---|---|---|
| 1 | 1451 KB | 2 | 3283 ／ 3958 |
| 2 | 1479 KB | 2 | 3456 ／ 3958 |
| 3 | 1594 KB | 2 | 4087 ／ 3958 |
| 4 | 1438 KB | 2 | 3225 ／ 3958 |
| 5 | 1523 KB | 2 | 3730 ／ 3958 |
| 6 | 1499 KB | 2 | 3557 ／ 3958 |
| 7 | 1504 KB | 2 | 3602 ／ 3958 |
| 8 | 1434 KB | 2 | 3208 ／ 3958 |
| 9 | 1499 KB | 2 | 3570 ／ 3958 |
| 10 | 1515 KB | 2 | 3657 ／ 3958 |

最低 3208 點，遠高於驗收門檻 300 點；兩個 target 都是 1200 × 1200。
總計 14.59 MB（每關約 1.5 MB，v1.2 為每關約 0.7 MB —— 萬用卡資料在十個檔案裡各存一份，
換來的是「每關只下載當關需要的一個檔」這個原本就有的好處不變）。

**功能驗證**

| 驗證項目 | 結果 |
|---|---|
| target 0（本關卡片）觸發 | ✅ `diag.foundIndex: 0`、橫幅「✓ 掃到第 N 號卡！」、進入**本關**教學 |
| target 1（萬用卡）觸發 | ✅ `diag.foundIndex: 1`、橫幅「✓ 掃到萬用卡！」、進入**本關**教學（第 6 關測得 `MICELLE REACTOR`） |
| 場景兩個錨點都在 | ✅ `#ar-target` = `{targetIndex:0}`、`#ar-target-uni` = `{targetIndex:1}` |
| **連續三關只掃萬用卡**（＝使用者回報的斷點場景） | ✅ 第 1／2／3 關連續通關，每關內容各自正確，中間**完全沒有換圖、沒有重新整理** |
| 每關結束相機確實關閉 | ✅ 三關皆 `AR.running: false`、`AR.stream: null`、無 live 的 MediaStreamTrack |
| `cards.html` 鍵盤 ←→ | ✅ 11 張（萬用卡＋10 關）、Home／End 跳頭尾、頭尾箭頭正確 disabled |
| `cards.html` 點左右半屏／箭頭／圓點 | ✅ 全部切換正確，標題同步為「關卡 N　關卡名」／「★ 遊戲萬用卡」 |
| `cards.html` 箭頭尺寸 | ✅ 64 × 64（規格要求 ≥64 px） |
| `cards.html` 記憶 | ✅ `localStorage['hgcq.cards.idx']` 記住上次那一張；`?n=` 可指定 |
| 免卡體驗：開相機 | ✅ 走 v1.1 預檢管線（亮度判定、12 秒寬限），`AR.sceneEl` 為 null（確認**沒有**用到 MindAR） |
| 免卡體驗：疊加內容 | ✅ 眉標／標題／1 張 SVG／3 條重點齊全，卡片 347 × 469 置中，浮動動畫只做位移 |
| 免卡體驗：版面不重疊 | ✅ 375 × 812 下卡片 `bottom 585` ＜ 按鈕列 `top 692`；按鈕列與「回到選單」皆完整落在畫面內 |
| **拍下學習瞬間** | ✅ 720 × 1280 直式相機合成 PNG，實景＋底板＋教學圖＋重點＋浮水印全部到齊 |
| 合成照片非空白 | ✅ 取樣亮度 min 42 / max 255（變化 > 60），PNG 70–91 KB |
| 合成版面自適應 | ✅ 10 關 × 最壞情況（640 × 480 橫式相機）全部合成成功，底部浮水印帶都沒有被內容擠掉 |
| 繼續 ▶ → 教學 | ✅ 相機 live track 由 1 → 0、疊加層收起、陀螺儀監聽解除、教學內容正確 |
| iOS 不彈陀螺儀權限視窗 | ✅ `DeviceOrientationEvent.requestPermission` 存在時直接不啟用視差（本機 Chrome 無此函式 → 走 Android 路徑，視差已掛上） |
| 模式記憶 | ✅ 選過的模式寫入 `localStorage['hgcq.scanMode']`，下一關該模式高亮並顯示「上次用這個」，其餘兩個不顯示徽章 |
| 模式記憶可覆寫 | ✅ 隨時改選另一個模式即更新記憶 |
| **隱私模式容錯** | ✅ 把 `localStorage` 改成一存取就丟 `SecurityError`：`loadMode()` 回 `null`、`saveMode()` 回 `false`、`markLastMode()`／`enterAR()` 正常執行、面板照樣可用，**零例外外洩** |
| 掃描面板文案 | ✅ 首行「用『萬用卡』可以一張玩到底；有十張卡片組的話，本關請掃第 N 號卡。」N 隨關卡變動 |
| 第二台裝置提示 | ✅ 掃描面板有一行連到 `cards.html` |
| 10 關完整回歸（圖片模式） | ✅ 10 關全過，每關教學 1 張 SVG＋重點、答對回饋、關卡結算；徽章、10 條回顧、5 個帶走概念齊全 |
| JavaScript 錯誤 | ✅ **全程零錯誤**（`error`／`unhandledrejection`／`console.warn` 皆為空；唯一的 warn 是 three.js 的 `useLegacyLights` 棄用提示，v1.0 起就存在，非本次回歸） |

### 順手修掉的坑：SVG 轉圖片時重複的 `xmlns`

「拍下學習瞬間」要把教學用的 SVG 畫進 canvas，作法是把 SVG 字串轉成 `data:` URL 再 `drawImage`。
第一版為了保險補了一個 `xmlns`，但 `VISUALS.svg()` 產出的字串本來就有 ——
**重複屬性在 XML 是致命解析錯誤**，圖片會安靜地什麼都不畫、也不丟例外，
合成照片就少了中間那張圖（截圖比對才發現）。已改成「缺了才補」。

同時把底板高度改成**先量再畫**：教學圖多高、重點幾行、相機是直式還是橫式每一關都不同，
量完若超過畫面就先縮圖、再砍掉最後幾行重點，確保照片不會有內容被裁掉。

### 順手修掉的坑：離線編譯器在背景分頁會永遠卡住

`tools/compile-each.html` 用瀏覽器跑 MindAR 的 Compiler。MindAR 每處理一個尺度層就
`await new Promise(r => requestAnimationFrame(r))`，而**分頁一旦不可見，
`requestAnimationFrame` 完全停擺**（`setTimeout` 也被夾到 1 秒一次），
編譯會卡在第一張圖不動，看起來像當掉。
已在編譯器頁面把 `requestAnimationFrame` 與短 `setTimeout` 改接 `MessageChannel`
（`postMessage` 不受可見度節流）。同一批 20 張圖的編譯從「15 分鐘還沒跑完第 1 關」
變成**約 30 秒跑完全部 10 關**。這只影響離線建置工具，遊戲本身不含這段程式。

---

## 三、無法在本機驗證的項目（**必須實機測試**）

| 項目 | 原因 |
|---|---|
| **實機相機開啟與後鏡頭選取** | 本機無可用相機（瀏覽器面板亦封鎖擷取） |
| **實際掃描列印圖片的辨識率與追蹤穩定度** | 需要真實相機 ＋ 實際列印稿 |
| **AR 3D 教學內容錨定在紙面上的視覺效果** | 同上（3D 內容本身已在場景中建立，但未經相機畫面驗證） |
| **iOS Safari 的 `playsinline` 實際行為** | 屬性已確認全數寫入（`playsinline` / `webkit-playsinline` / `muted` / `autoplay` 皆為 true），但 iOS 是否全螢幕接管需實機確認 |
| **HTTPS 下的相機權限流程** | 本機走 `localhost` 例外；正式環境需 HTTPS |
| **實機效能（追蹤幀率、發熱）** | 需實機 |
| **Android 各廠牌瀏覽器差異** | 需實機 |

---

## 四、手機測試檢查表（給使用者）

### 測試前準備

- [ ] 用 A4 紙**以實際大小（100%，勿縮放）**列印 `print/關鍵圖片列印稿.pdf`（共 5 頁、10 張圖）
- [ ] 沿灰色外框把每張圖裁下（或整頁使用亦可）
- [ ] 把圖平放在**光線充足、不反光**的桌面上（避免玻璃桌面與強烈直射光）
- [ ] 網址必須是 **https://**（GitHub Pages／Netlify 皆符合）；`http://` 不會給相機權限
- [ ] **分享連結時請用這個網址**（LINE 點開會自動跳外部瀏覽器，相機才能用）：
      `https://rlirdo.github.io/FCThesis_Serpentine-DSW/?openExternalBrowser=1`
- [ ] **不要**在 LINE／Facebook／Instagram 的內建瀏覽器裡測試相機 —— 那些 App 會封鎖相機
- [ ] 確認開場頁右下角顯示 **v1.1**；若不是，請強制重新整理清掉快取

### Android（Chrome）

- [ ] 開啟網址，載入畫面正常結束，進入標題頁
- [ ] 選主角 → 選 5 關 → 進入第 1 關
- [ ] 方向鍵可移動主角，走到金色目標點時出現「開始掃描」
- [ ] 按「開始掃描（開啟相機）」，瀏覽器詢問相機權限 → 點**允許**
- [ ] 畫面出現**後鏡頭**影像（不是自拍鏡頭）
- [ ] 對準關卡 1 的圖，讓整張圖填滿取景框 → 教學內容浮現
- [ ] 答題 → 過關 → 進入下一關
- [ ] 走完 5 關 → 出現徽章與五大概念

### iPhone（Safari）

- [ ] 同上全部項目
- [ ] **重點確認**：開啟相機後畫面**沒有被 iOS 全螢幕播放器接管**（應該是嵌在遊戲畫面裡）
- [ ] **重點確認**：相機是**後鏡頭**
- [ ] 直式握持時 UI 不被瀏海或底部橫條遮住
- [ ] 按「分享我的完成徽章」可叫出 iOS 分享選單

### 降級路徑（每支手機都要測一次）

- [ ] 在權限詢問時故意選「**不允許**」→ 應自動出現「沒有相機也能繼續」面板
- [ ] 按「用圖片模式繼續」→ 可看到關鍵圖片與完整教學內容
- [ ] 按「跳過掃描，直接答題」→ 可正常答題過關
- [ ] 按「再試一次相機」→ 可重新嘗試

### 辨識不順時的調整順序

1. 讓圖**填滿更多畫面**（靠近一點，但不要近到失焦）
2. 換到**光線更均勻**的地方（避免陰影橫跨圖面、避免反光）
3. 把紙**壓平**（捲曲或翹起會讓追蹤不穩）
4. 手機**拿穩**兩秒不要晃
5. 若列印顏色太淡，改用較高品質列印或雷射印表機

---

## 四之二、相機故障診斷流程圖

**先確認一件事：你是從哪裡點開網址的？** 八成以上的「掃描框全黑」都在這一步解決。

```
                     ┌──────────────────────────────┐
                     │ 按「開始掃描（開啟相機）」    │
                     └───────────────┬──────────────┘
                                     ▼
                  ┌──────────────────────────────────────┐
                  │ 頁面最上方有紅色橫幅嗎？             │
                  │（「你正在『LINE／FB／IG』的內建…」） │
                  └───────┬──────────────────┬───────────┘
                       有 │                  │ 沒有
                          ▼                  ▼
        ┌─────────────────────────┐   ┌──────────────────────────────┐
        │ ★ 這就是原因            │   │ 掃描框內看得到影像嗎？        │
        │ 內建瀏覽器封鎖相機      │   └──┬────────────────┬──────────┘
        │                         │   有 │                │ 全黑
        │ LINE：v1.1 會自動跳外部 │      ▼                ▼
        │  瀏覽器；沒跳就點右下   │  ┌────────┐   ┌───────────────────────┐
        │  角「⋯」→ 用其他瀏覽器 │  │ 正常   │   │ 有出現診斷面板嗎？     │
        │ FB/IG：右上角選單 →     │  │ 對準圖 │   │（「相機無法使用」）    │
        │  「以瀏覽器開啟」       │  │ 掃描   │   └──┬─────────────┬──────┘
        └─────────────────────────┘  └────────┘   有 │             │ 沒有／
                                                     ▼             ▼ 一直轉
                                          ┌──────────────────┐  ┌────────────────┐
                                          │ 讀「原因：」那行 │  │ 等倒數跑完     │
                                          └────────┬─────────┘  │（12 秒），或按 │
                                                   │            │「繼續等待」再等│
              ┌────────────────────────────────────┤            └────────────────┘
              ▼                ▼                   ▼
   ┌──────────────────┐ ┌──────────────┐ ┌────────────────────────┐
   │「權限被拒絕」    │ │「鏡頭被其他  │ │「畫面持續全黑」        │
   │                  │ │  App 佔用」  │ │                        │
   │ iPhone：設定 →   │ │              │ │ ・鏡頭有保護蓋／貼紙？ │
   │  Safari → 相機   │ │ 關掉相機 App │ │ ・手指擋住鏡頭？       │
   │  → 允許          │ │ 視訊通話、其 │ │ ・環境太暗？開燈再試   │
   │ Android：網址列  │ │ 他分頁，再按 │ │ ・仍全黑 → 改用        │
   │  鎖頭 → 權限 →   │ │「重試相機」  │ │   「圖片模式」照樣過關 │
   │  相機 → 允許     │ │              │ │                        │
   │ 然後重新整理     │ │              │ │                        │
   └──────────────────┘ └──────────────┘ └────────────────────────┘
              │                │                   │
              └────────────────┴───────────────────┘
                               ▼
              ┌────────────────────────────────────────┐
              │ 還是不行 → 按「📋 複製診斷資訊」        │
              │ 貼回給製作者（含 UA／權限／錯誤／亮度） │
              │ 同時：遊戲用「圖片模式」或「直接答題」  │
              │ 一樣能完整玩完，不會卡關               │
              └────────────────────────────────────────┘
```

### 三個永遠可用的備援

無論相機能不能開，這三條路都能把 10 關完整走完：

| 按鈕 | 效果 |
|---|---|
| **重試相機** | 重跑一次完整的預檢流程（含權限詢問） |
| **用圖片模式繼續** | 直接在畫面上看關鍵圖片 ＋ 完整教學內容，再答題 |
| **直接答題** | 跳過掃描與圖片，直接進入該關問答 |

---

## 四之三、使用者回報範本

若相機仍然打不開，請照下面格式回報（**第 5 項最重要**）：

```
【花蓮綠色化學闖關 — 相機問題回報】

1. 手機型號：            （例：iPhone 13 / Samsung S23）
2. 系統版本：            （例：iOS 17.5 / Android 14）
3. 我是從哪裡點開網址的：（例：LINE 訊息 / QR Code / 自己輸入網址 / Facebook）
4. 我用哪個瀏覽器：      （例：Safari / Chrome / LINE 內建 / 不確定）
5. 【診斷資訊】——按畫面上的「📋 複製診斷資訊」後貼在這裡：

   （貼上）

6. 畫面上看到什麼：
   □ 完全沒反應          □ 有跳出權限詢問，我按了允許／不允許
   □ 黃色掃描框內全黑    □ 出現「相機無法使用」面板
   □ 有影像但掃不到圖    □ 其他：__________

7. 頁面最上方有沒有紅色橫幅？ □ 有（寫上內容：______） □ 沒有
8. 開場頁右下角的版號是？     □ v1.1  □ 其他：______  □ 沒看到
```

> **給回報者的提醒**：第 5 項的診斷資訊會自動帶入 UA、相機權限狀態、錯誤名稱、
> 影像尺寸與畫面平均亮度 —— 這幾項合起來幾乎就能定位問題，比文字描述有效得多。
> 若按鈕複製失敗，畫面會出現一個文字方塊，長按全選複製即可。
>
> **第 8 項**用來確認你拿到的是修正版：若不是 `v1.1`，請**強制重新整理**
> （iPhone Safari：長按重新整理鍵 → 重新載入而不使用快取；Android Chrome：
> 設定 → 隱私權 → 清除瀏覽資料 → 快取），或直接關掉分頁重開。

---

## 五、發布前需要使用者決定的事項

1. **發布平台**：GitHub Pages（`rlirdo` 帳號）或 Netlify？兩者皆為 HTTPS。
2. **repo 名稱與公開性**：例如 `hualien-green-chemistry-ar`；GitHub Pages 需公開 repo。
3. **專案體積**：目前約 14.2 MB（`targets/` 6.9 MB ＋ `vendor/` 3.1 MB ＋ 列印稿 3.1 MB）。
   是否要把 `print/關鍵圖片列印稿.pdf`（3.1 MB）排除在網站之外、改為另行提供？
4. **關鍵圖片是否要加上單位識別**（例如系徽或活動名稱），若要，需重新產圖並重新編譯 `.mind`。
5. **是否需要 QR Code**：可另做一張含網址 QR 的海報，方便現場學生掃描進入。
6. **10 關內容是否需要指導教授審閱**後再發布（尤其第 9 關 CO₂ 礦化屬延伸假說的措辭）。

---

## 六、附註：本機執行方式

```bash
cd "D:\01-FC東華\04-專案\AR遊戲_花蓮綠色化學闖關"
python tools/devserver.py 8765
# 瀏覽器開 http://localhost:8765
```

同一 Wi-Fi 下若要用手機測試本機版，需注意：**手機連本機 IP 屬非安全來源，
相機不會啟動**。實機相機測試請等發布到 HTTPS 網址之後再進行。
